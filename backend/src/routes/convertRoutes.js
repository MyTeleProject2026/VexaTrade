// backend/src/routes/convertRoutes.js
const express=require('express');const router=express.Router();const crypto=require('crypto');const pool=require('../../db');const {authUser}=require('../middleware/auth');const {transactionSecurity}=require('../middleware/transactionSecurity');const {createError,getSupportedConvertCoins,createTransactionLog,createUserNotification}=require('../utils/helpers');const {getBinancePrice}=require('../../services/tradeService');const {debitAvailableAsset,creditAssetBalance}=require('../../services/assetLedgerService');const getIdempotencyKey=req=>String(req.get('Idempotency-Key')||req.body.idempotencyKey||'').trim().slice(0,128);const requestHash=(fromCoin,toCoin,fromAmount)=>crypto.createHash('sha256').update(`${fromCoin}|${toCoin}|${fromAmount}`).digest('hex');router.post('/convert/execute',authUser,transactionSecurity('convert'),async(req,res,next)=>{const c=await pool.getConnection();try{const fromCoin=String(req.body.fromCoin||'').trim().toUpperCase(),toCoin=String(req.body.toCoin||'').trim().toUpperCase(),fromAmount=Number(req.body.fromAmount||0);let idempotencyKey=getIdempotencyKey(req);const supported=getSupportedConvertCoins();if(!fromCoin||!toCoin||fromCoin===toCoin)throw createError(400,'Invalid coin selection');if(!supported.includes(fromCoin)||!supported.includes(toCoin))throw createError(400,'Unsupported coin');if(!Number.isFinite(fromAmount)||fromAmount<=0)throw createError(400,'Invalid amount');if(!idempotencyKey)idempotencyKey=crypto.randomUUID();const hash=requestHash(fromCoin,toCoin,fromAmount);const [replayRows]=await pool.execute('SELECT id,from_coin,to_coin,from_amount,from_price_usdt,to_price_usdt,gross_usdt_value,fee_percent,fee_usdt,net_usdt_value,receive_amount,status,request_hash FROM convert_transactions WHERE user_id=? AND idempotency_key=? LIMIT 1',[req.user.id,idempotencyKey]);if(replayRows.length){if(replayRows[0].request_hash!==hash)throw createError(409,'Idempotency-Key was already used for a different conversion request');return res.json({success:true,message:'Existing conversion returned',data:{id:replayRows[0].id,fromCoin:replayRows[0].from_coin,toCoin:replayRows[0].to_coin,fromAmount:replayRows[0].from_amount,fromPriceUsdt:replayRows[0].from_price_usdt,toPriceUsdt:replayRows[0].to_price_usdt,grossUsdtValue:replayRows[0].gross_usdt_value,feePercent:replayRows[0].fee_percent,feeUsdt:replayRows[0].fee_usdt,netUsdtValue:replayRows[0].net_usdt_value,receiveAmount:replayRows[0].receive_amount,replayed:true,status:replayRows[0].status}});}
    // External market requests must happen before the DB transaction so a slow
    // Binance response never holds user balance/ledger locks.
    let fp,tp;
    try {
      fp=fromCoin==='USDT'?1:Number(await getBinancePrice(`${fromCoin}USDT`));
      tp=toCoin==='USDT'?1:Number(await getBinancePrice(`${toCoin}USDT`));
    } catch (_) {
      throw createError(503,'Conversion market price is temporarily unavailable. Please try again.');
    }
    if(!Number.isFinite(fp)||fp<=0||!Number.isFinite(tp)||tp<=0)throw createError(503,'Conversion market price is temporarily unavailable. Please try again.');
    await c.beginTransaction();const [u]=await c.execute('SELECT id,status FROM users WHERE id=? FOR UPDATE',[req.user.id]);if(!u.length)throw createError(404,'User not found');if(['disabled','frozen'].includes(String(u[0].status||'').toLowerCase()))throw createError(403,'User not active');const [existing]=await c.execute('SELECT id,from_coin,to_coin,from_amount,from_price_usdt,to_price_usdt,gross_usdt_value,fee_percent,fee_usdt,net_usdt_value,receive_amount,status,request_hash FROM convert_transactions WHERE user_id=? AND idempotency_key=? LIMIT 1 FOR UPDATE',[req.user.id,idempotencyKey]);if(existing.length){if(existing[0].request_hash!==hash)throw createError(409,'Idempotency-Key was already used for a different conversion request');await c.commit();return res.json({success:true,message:'Existing conversion returned',data:{id:existing[0].id,fromCoin:existing[0].from_coin,toCoin:existing[0].to_coin,fromAmount:existing[0].from_amount,fromPriceUsdt:existing[0].from_price_usdt,toPriceUsdt:existing[0].to_price_usdt,grossUsdtValue:existing[0].gross_usdt_value,feePercent:existing[0].fee_percent,feeUsdt:existing[0].fee_usdt,netUsdtValue:existing[0].net_usdt_value,receiveAmount:existing[0].receive_amount,replayed:true,status:existing[0].status}})}let feePct=.2;const [sr]=await c.execute("SELECT setting_value FROM platform_settings WHERE setting_key='default_convert_fee_percent' LIMIT 1");if(sr.length)feePct=Number(sr[0].setting_value||.2);const gross=Number((fromAmount*fp).toFixed(8)),fee=Number((gross*feePct/100).toFixed(8)),net=Number((gross-fee).toFixed(8)),receive=Number((net/tp).toFixed(8));if(receive<=0)throw createError(400,'Invalid receive amount');const [result]=await c.execute(`INSERT INTO convert_transactions(user_id,from_coin,to_coin,from_amount,from_price_usdt,to_price_usdt,gross_usdt_value,fee_percent,fee_usdt,net_usdt_value,receive_amount,status,idempotency_key,request_hash,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,'completed',?,?,NOW(),NOW())`,[req.user.id,fromCoin,toCoin,fromAmount,fp,tp,gross,feePct,fee,net,receive,idempotencyKey,hash]);await debitAvailableAsset(c,{userId:req.user.id,coin:fromCoin,network:'INTERNAL',amount:fromAmount,referenceType:'convert',referenceId:result.insertId,note:`Convert ${fromCoin} to ${toCoin}`});await creditAssetBalance(c,{userId:req.user.id,coin:toCoin,network:'INTERNAL',amount:receive,referenceType:'convert',referenceId:result.insertId,note:`Converted ${fromCoin} to ${toCoin}`});await createTransactionLog(c,{userId:req.user.id,type:'convert',amount:gross,status:'completed',referenceId:result.insertId,note:`Converted ${fromAmount} ${fromCoin} to ${receive} ${toCoin}`});await createUserNotification(c,{userId:req.user.id,title:'Convert completed',message:`Converted ${fromAmount} ${fromCoin} to ${receive} ${toCoin}. Fee: ${fee} USDT.`,type:'convert'});await c.commit();res.json({success:true,message:'Conversion completed',data:{id:result.insertId,fromCoin,toCoin,fromAmount,fromPriceUsdt:fp,toPriceUsdt:tp,grossUsdtValue:gross,feePercent:feePct,feeUsdt:fee,netUsdtValue:net,receiveAmount:receive,status:'completed'}})}catch(e){try{await c.rollback()}catch(_){}next(e)}finally{c.release()}});router.get('/convert/history',authUser,async(req,res,next)=>{try{const [rows]=await pool.execute('SELECT * FROM convert_transactions WHERE user_id=? ORDER BY id DESC LIMIT 200',[req.user.id]);res.json({success:true,data:rows})}catch(e){next(e)}});module.exports=router;