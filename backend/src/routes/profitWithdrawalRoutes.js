const express=require('express');const crypto=require('crypto');const router=express.Router();const pool=require('../../db');const {authUser,authAdmin}=require('../middleware/auth');const {transactionSecurity}=require('../middleware/transactionSecurity');const {createError,createTransactionLog,createUserNotification,createAuditLog}=require('../utils/helpers');const idempotencyKey=req=>String(req.get('Idempotency-Key')||req.body?.idempotencyKey||'').trim().slice(0,128);const requestHash=amount=>crypto.createHash('sha256').update(String(amount)).digest('hex');let readyPromise=null;async function ensureTable(){if(!readyPromise){readyPromise=pool.execute(`CREATE TABLE IF NOT EXISTS profit_withdrawal_requests (id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,user_id BIGINT UNSIGNED NOT NULL,amount DECIMAL(36,18) NOT NULL,status VARCHAR(32) NOT NULL DEFAULT 'pending',request_hash CHAR(64) NULL,idempotency_key VARCHAR(128) NULL,admin_note TEXT NULL,created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,approved_at DATETIME NULL,rejected_at DATETIME NULL,PRIMARY KEY(id),UNIQUE KEY uq_profit_withdrawal_user_key(user_id,idempotency_key),KEY idx_profit_withdrawal_user_status(user_id,status),KEY idx_profit_withdrawal_status_created(status,created_at)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`).finally(()=>{readyPromise=null});}await readyPromise;}async function getSettings(connection){try{const [rows]=await connection.execute(`SELECT min_withdrawal_from_profit,max_withdrawal_from_profit,allow_withdrawal_before_target,restriction_message FROM platform_withdrawal_settings WHERE id=1 LIMIT 1`);return rows[0]||{};}catch(_){return {};}}async function availableProfit(connection,userId){const [[target]]=await connection.execute(`SELECT id,target_amount,current_profit,status FROM user_targets WHERE user_id=? AND status='active' ORDER BY id DESC LIMIT 1 FOR UPDATE`,[userId]);const [[pending]]=await connection.execute(`SELECT COALESCE(SUM(amount),0) pending_amount FROM profit_withdrawal_requests WHERE user_id=? AND status='pending'`,[userId]);return{target:target||null,currentProfit:Number(target?.current_profit||0),pendingProfit:Number(pending?.pending_amount||0),available:Math.max(0,Number(target?.current_profit||0)-Number(pending?.pending_amount||0))};}
router.post('/withdraw/profit-request',authUser,transactionSecurity('profit-withdrawal'),async(req,res,next)=>{const db=await pool.getConnection();try{await ensureTable();const amount=Number(req.body?.amount),key=idempotencyKey(req);if(!Number.isFinite(amount)||amount<=0)throw createError(400,'Valid profit withdrawal amount required');if(!key)throw createError(400,'Idempotency-Key header is required');await db.beginTransaction();const settings=await getSettings(db),min=Number(settings.min_withdrawal_from_profit||0),max=settings.max_withdrawal_from_profit==null?null:Number(settings.max_withdrawal_from_profit);if(min>0&&amount<min)throw createError(400,`Minimum profit withdrawal is ${min} USDT`);if(max!=null&&Number.isFinite(max)&&amount>max)throw createError(400,`Maximum profit withdrawal is ${max} USDT`);if(Number(settings.allow_withdrawal_before_target||0)!==1)throw createError(403,settings.restriction_message||'Profit withdrawals are not currently allowed before the target is achieved');const hash=requestHash(amount);const [existing]=await db.execute(`SELECT id,amount,status,request_hash FROM profit_withdrawal_requests WHERE user_id=? AND idempotency_key=? LIMIT 1 FOR UPDATE`,[req.user.id,key]);if(existing.length){if(existing[0].request_hash!==hash)throw createError(409,'Idempotency-Key was already used for a different profit withdrawal');await db.commit();return res.json({success:true,message:'Existing profit withdrawal request returned',data:{...existing[0],replayed:true}});}const funds=await availableProfit(db,req.user.id);if(!funds.target)throw createError(400,'No active target with available profit');if(amount>funds.available)throw createError(400,`Only ${funds.available.toFixed(8)} USDT of profit is currently available for withdrawal`);const [result]=await db.execute(`INSERT INTO profit_withdrawal_requests(user_id,amount,status,request_hash,idempotency_key,created_at,updated_at) VALUES(?,?, 'pending',?,?,NOW(),NOW())`,[req.user.id,amount,hash,key]);await createTransactionLog(db,{userId:req.user.id,type:'profit_withdrawal_request',amount,status:'pending',referenceId:result.insertId,note:'Profit withdrawal submitted for admin review'});await createUserNotification(db,{userId:req.user.id,title:'Profit withdrawal submitted',message:`Your ${amount.toFixed(2)} USDT profit withdrawal request is pending admin review.`,type:'withdrawal'});await db.commit();res.json({success:true,message:'Profit withdrawal request submitted for review',data:{id:result.insertId,amount,status:'pending',currentProfit:funds.currentProfit,pendingProfit:funds.pendingProfit+amount,availableProfit:funds.available-amount}});}catch(e){try{await db.rollback();}catch(_){}next(e);}finally{db.release();}});router.get('/withdraw/profit-history',authUser,async(req,res,next)=>{try{await ensureTable();const [rows]=await pool.execute(`SELECT id,amount,status,admin_note,created_at,updated_at,approved_at,rejected_at FROM profit_withdrawal_requests WHERE user_id=? ORDER BY id DESC LIMIT 100`,[req.user.id]);res.json({success:true,data:rows});}catch(e){next(e);}});router.get('/admin/profit-withdrawal-requests',authAdmin,async(req,res,next)=>{try{await ensureTable();const [rows]=await pool.execute(`SELECT p.id,p.user_id,p.amount,p.status,p.admin_note,p.created_at,p.updated_at,p.approved_at,p.rejected_at,u.uid,u.name,u.email,COALESCE((SELECT current_profit FROM user_targets t WHERE t.user_id=p.user_id AND t.status='active' ORDER BY t.id DESC LIMIT 1),0) current_profit FROM profit_withdrawal_requests p LEFT JOIN users u ON u.id=p.user_id ORDER BY p.id DESC LIMIT 200`);res.json({success:true,data:rows});}catch(e){next(e);}});router.post('/admin/profit-withdrawal-requests/:id/approve',authAdmin,async(req,res,next)=>{const db=await pool.getConnection();try{await ensureTable();const requestId=Number(req.params.id);if(!Number.isInteger(requestId)||requestId<=0)throw createError(400,'Invalid request ID');await db.beginTransaction();const [[request]]=await db.execute(`SELECT * FROM profit_withdrawal_requests WHERE id=? FOR UPDATE`,[requestId]);if(!request)throw createError(404,'Profit withdrawal request not found');if(request.status!=='pending')throw createError(409,`Request is already ${request.status}`);const funds=await availableProfit(db,request.user_id);if(!funds.target)throw createError(409,'User has no active target');if(Number(request.amount)>funds.available)throw createError(409,'Requested profit is no longer available');const [updatedTarget]=await db.execute(`UPDATE user_targets SET current_profit=current_profit-?,updated_at=NOW() WHERE id=? AND current_profit>=?`,[request.amount,funds.target.id,request.amount]);if(!updatedTarget.affectedRows)throw createError(409,'Requested profit is no longer available');await db.execute(`UPDATE profit_withdrawal_requests SET status='approved',approved_at=NOW(),updated_at=NOW() WHERE id=?`,[requestId]);await createTransactionLog(db,{userId:request.user_id,type:'profit_withdrawal_approved',amount:Number(request.amount),status:'approved',referenceId:requestId,note:'Profit withdrawal approved for manual treasury settlement'});await createUserNotification(db,{userId:request.user_id,title:'Profit withdrawal approved',message:`Your ${Number(request.amount).toFixed(2)} USDT profit withdrawal was approved and is ready for manual settlement.`,type:'withdrawal'});await createAuditLog(db,{adminId:req.admin.id,action:'approve_profit_withdrawal',note:`Approved profit withdrawal request ${requestId}`});await db.commit();res.json({success:true,message:'Profit withdrawal approved',data:{id:requestId,status:'approved',amount:Number(request.amount)}});}catch(e){try{await db.rollback();}catch(_){}next(e);}finally{db.release();}});router.post('/admin/profit-withdrawal-requests/:id/reject',authAdmin,async(req,res,next)=>{const db=await pool.getConnection();try{await ensureTable();const requestId=Number(req.params.id);if(!Number.isInteger(requestId)||requestId<=0)throw createError(400,'Invalid request ID');await db.beginTransaction();const [[request]]=await db.execute(`SELECT * FROM profit_withdrawal_requests WHERE id=? FOR UPDATE`,[requestId]);if(!request)throw createError(404,'Profit withdrawal request not found');if(request.status!=='pending')throw createError(409,`Request is already ${request.status}`);const note=String(req.body?.note||req.body?.admin_note||'').trim().slice(0,2000)||null;await db.execute(`UPDATE profit_withdrawal_requests SET status='rejected',admin_note=?,rejected_at=NOW(),updated_at=NOW() WHERE id=?`,[note,requestId]);await createTransactionLog(db,{userId:request.user_id,type:'profit_withdrawal_rejected',amount:Number(request.amount),status:'rejected',referenceId:requestId,note:note||'Profit withdrawal rejected by administrator'});await createUserNotification(db,{userId:request.user_id,title:'Profit withdrawal rejected',message:note?`Your profit withdrawal was rejected: ${note}`:'Your profit withdrawal request was rejected by an administrator.',type:'withdrawal'});await createAuditLog(db,{adminId:req.admin.id,action:'reject_profit_withdrawal',note:`Rejected profit withdrawal request ${requestId}`});await db.commit();res.json({success:true,message:'Profit withdrawal rejected',data:{id:requestId,status:'rejected'}});}catch(e){try{await db.rollback();}catch(_){}next(e);}finally{db.release();}});module.exports=router;