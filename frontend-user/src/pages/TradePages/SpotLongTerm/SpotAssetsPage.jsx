import { useEffect,useState } from "react";
import { Wallet, RefreshCw } from "lucide-react";
import { userApi } from "../../../services/api";
import TradeSectionLayout from "../TradeSectionLayout";
const token=()=>localStorage.getItem("userToken")||localStorage.getItem("token")||localStorage.getItem("accessToken")||"";
export default function SpotAssetsPage(){const [assets,setAssets]=useState([]),[loading,setLoading]=useState(true);const load=async()=>{setLoading(true);try{const r=await userApi.getUserAssets(token());const d=r.data?.data;setAssets(Array.isArray(d?.assets)?d.assets:Array.isArray(d)?d:[])}finally{setLoading(false)}};useEffect(()=>{load()},[]);return <TradeSectionLayout title="Spot Assets" subtitle="Balances available to Spot execution" mode="spot"><section className="rounded-2xl border border-white/10 bg-[#0a0e1a]/90 p-3 shadow-[0_12px_35px_rgba(0,0,0,0.2)]"><div className="flex items-center justify-between"><div className="flex items-center gap-2 text-xs font-semibold"><Wallet size={15} className="text-cyan-300"/>Balances</div><button onClick={load} className="rounded-xl border border-white/10 bg-white/[0.03] p-2 text-slate-400 transition hover:border-cyan-300/20 hover:bg-cyan-300/[0.05] hover:text-cyan-200 active:scale-[0.98]"><RefreshCw size={13} className={loading?"animate-spin":""}/></button></div><div className="mt-3 space-y-2">{!loading&&!assets.length&&<div className="py-10 text-center text-[10px] text-slate-600">No asset balances returned.</div>}{assets.map((a,i)=><div key={a.coin||a.symbol||i} className="flex items-center justify-between rounded-xl bg-[#050812] p-3"><div><div className="text-xs font-semibold">{a.coin||a.symbol||"—"}</div><div className="mt-1 text-[9px] text-slate-500">Available</div></div><div className="text-right text-xs">{a.available_balance??a.availableBalance??a.balance??"0"}</div></div>)}</div></section></TradeSectionLayout>}
