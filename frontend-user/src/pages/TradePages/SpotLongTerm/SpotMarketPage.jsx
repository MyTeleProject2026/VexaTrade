import { useEffect, useState } from "react";
import { useNavigate } from "react-router-dom";
import MarketChart from "../../../components/MarketChart";
import OrderBook from "../../../components/OrderBook";
import TradeSectionLayout from "../TradeSectionLayout";
const PAIRS=["BTCUSDT","ETHUSDT","SOLUSDT","BNBUSDT","XRPUSDT","DOGEUSDT","ADAUSDT","AVAXUSDT","LINKUSDT"];
export default function SpotMarketPage(){const [pair,setPair]=useState("BTCUSDT");const [price,setPrice]=useState(0);const navigate=useNavigate();useEffect(()=>{let closed=false,ws;try{ws=new WebSocket("wss://stream.binance.com:9443/ws/"+pair.toLowerCase()+"@ticker");ws.onmessage=e=>{try{const p=Number(JSON.parse(e.data)?.c);if(!closed&&p>0)setPrice(p)}catch{}}}catch{}return()=>{closed=true;try{ws?.close()}catch{}}},[pair]);return <TradeSectionLayout title="Spot Market" subtitle="Live market execution view" mode="spot"><div className="space-y-2.5"><div className="flex gap-1.5 overflow-x-auto pb-1">{PAIRS.map(p=><button key={p} onClick={()=>setPair(p)} className={`vexa-spot-touch-target shrink-0 rounded-xl border px-3 py-2 text-[10px] font-semibold transition ${pair===p?"border-cyan-300/30 bg-cyan-300/10 text-cyan-200":"border-white/10 bg-white/[0.02] text-slate-500 hover:border-cyan-300/15"}`}>{p}</button>)}</div><MarketChart symbol={pair} interval="5m" height={300}/><OrderBook symbol={pair} currentPrice={price}/><button onClick={()=>navigate("/trade/spot/long-term/order")} className="vexa-spot-touch-target w-full rounded-xl bg-cyan-400 py-3.5 text-sm font-bold text-black sm:text-base">Open Spot Order</button></div></TradeSectionLayout>}
