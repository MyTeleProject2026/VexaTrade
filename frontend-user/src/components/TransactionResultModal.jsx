import { useEffect, useState } from "react";
import { CheckCircle2, Clock3, X, Copy } from "lucide-react";
const LABELS={deposit:"Deposit",withdrawal:"Withdrawal",convert:"Conversion",transfer:"Transfer",trade:"Trade",funds:"Trading Funds",loan:"Loan", "profit-withdrawal":"Profit Withdrawal"};
const STAGES=["Configure","Details","Review","Security","Processing","Complete"];
function flatten(data){if(!data||typeof data!=="object")return[];return Object.entries(data).filter(([key,value])=>value!==undefined&&value!==null&&typeof value!=="object"&&key!=="replayed").slice(0,14);}
export default function TransactionResultModal(){const [item,setItem]=useState(null);const [flow,setFlow]=useState(null);useEffect(()=>{const complete=e=>{const detail=e.detail||{};setItem(detail);setFlow({action:detail.action||"transaction",phase:"complete"});};const progress=e=>setFlow(e.detail||null);window.addEventListener("vexa:transaction-complete",complete);window.addEventListener("vexa:transaction-flow",progress);return()=>{window.removeEventListener("vexa:transaction-complete",complete);window.removeEventListener("vexa:transaction-flow",progress);};},[]);const action=flow?.action||item?.action||"transaction";const phase=flow?.phase||"complete";const phaseIndex=phase==="security"?3:phase==="processing"?4:phase==="complete"?5:phase==="review"?2:0;const title=LABELS[action]||"Transaction";const data=item?.data||{};const status=String(data.status||item?.status||"completed");const rows=flatten(data);const dismiss=()=>{setItem(null);setFlow(null);};return <>{flow&&<div className="pointer-events-none fixed inset-x-0 top-2 z-[92] flex justify-center px-3"><div className="pointer-events-auto w-full max-w-3xl rounded-2xl border border-white/10 bg-[#081223]/95 px-3 py-2 shadow-2xl backdrop-blur-xl"><div className="flex items-center justify-between gap-3"><div><div className="text-[9px] font-semibold uppercase tracking-[0.25em] text-cyan-300">Secure transaction</div><div className="text-xs font-semibold text-white">{title} · {STAGES[phaseIndex]}</div></div>{phase==="processing"&&<Clock3 size={16} className="animate-pulse text-cyan-300"/>}{phase==="complete"&&<CheckCircle2 size={16} className="text-emerald-400"/>}</div><div className="mt-2 grid grid-cols-6 gap-1">{STAGES.map((name,index)=><div key={name}><div className={`h-1.5 rounded-full ${index<=phaseIndex?"bg-cyan-400":"bg-white/10"}`}/><div className="mt-1 hidden truncate text-[8px] text-slate-500 sm:block">{name}</div></div>)}</div>{phase==="processing"&&<div className="mt-2 text-[10px] text-slate-500">Your financial request is being sent once. Keep this page open.</div>}</div></div>}{item&&<div className="fixed inset-0 z-[95] flex items-center justify-center bg-black/85 p-4 backdrop-blur-md"><div className="max-h-[92vh] w-full max-w-lg overflow-auto rounded-[30px] border border-white/10 bg-[#081223] p-5 shadow-2xl"><div className="flex items-center justify-between"><div><div className="text-[10px] uppercase tracking-[0.3em] text-cyan-300">VexaTrade transaction receipt</div><h2 className="mt-1 text-2xl font-bold text-white">{title}</h2></div><button onClick={dismiss} className="rounded-xl p-2 text-slate-400"><X size={20}/></button></div><div className="mt-5 rounded-2xl border border-white/10 bg-black/20 p-5 text-center"><CheckCircle2 className="mx-auto text-emerald-400" size={34}/><div className="mt-2 text-xl font-bold uppercase text-white">{status}</div><div className="mt-1 text-sm text-slate-400">The operation completed with one controlled API request.</div></div><div className="mt-5 space-y-2">{rows.map(([key,value])=><div key={key} className="flex justify-between gap-4 border-b border-white/5 py-2 text-sm"><span className="text-slate-500">{key.replaceAll("_"," ")}</span><span className="max-w-[65%] break-all text-right font-semibold text-white">{String(value)}</span></div>)}</div><div className="mt-5 grid grid-cols-2 gap-2"><button onClick={()=>{const text=JSON.stringify(data,null,2);navigator.clipboard?.writeText(text);}} className="rounded-2xl border border-white/10 bg-white/[0.03] py-3 text-sm font-semibold text-white"><Copy size={15} className="mr-2 inline"/>Copy Receipt</button><button onClick={dismiss} className="rounded-2xl bg-cyan-500 py-3 text-sm font-semibold text-black">Done</button></div></div></div>}</>}
